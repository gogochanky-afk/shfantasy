{
  "name": "shfantasy",
  "version": "1.0.0",
  "description": "SHFantasy - NBA Daily Fantasy (Pool-based)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "sync:schedule": "node -e \"require('./scripts/sync-schedule').syncSchedule({dbPath: (process.env.DB_PATH || './shfantasy.db')}).then(console.log).catch(e=>{console.error(e);process.exit(1);})\""
  },
  "dependencies": {
    "axios": "^1.13.5",
    "better-sqlite3": "^12.6.2",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
