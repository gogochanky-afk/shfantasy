const express = require("express");
const router = express.Router();

// Placeholder (Phase ② will fill logic)
router.get("/", async (req, res) => {
  res.json({
    ok: true,
    message: "pools endpoint ready (logic coming in Phase ②)"
  });
});

module.exports = router;
